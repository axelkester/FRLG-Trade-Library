# Web application (webapp/) dependencies - install IN ADDITION to requirements.txt.
# Tested with Python 3.10+ (repo core requires 3.12+ for the live LDN stack).
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
tomli>=2.0; python_version < "3.11"
httpx>=0.27
pytest>=8.0
